import React from 'react';
import './Header.scss';

export const Header = () => {
  return (
    <header>
      <input className="searchBar" type="search" placeholder="Search bar design guide ..." autoComplete="off" />
    </header>
  );
};
